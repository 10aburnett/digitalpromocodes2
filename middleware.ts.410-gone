// DEPLOY THIS AFTER robots.txt DISALLOW AND GOOGLE REMOVALS TOOL
// This returns HTTP 410 Gone for all routes to accelerate final deindexing

import { NextResponse } from 'next/server';

export function middleware() {
  return new NextResponse('Gone', { status: 410 });
}

export const config = {
  matcher: '/:path*',
};
